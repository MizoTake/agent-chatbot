{
  "app": {
    "name": "Agent Chatbot",
    "version": "1.0.0",
    "environment": "development"
  },
  "claude": {
    "command": "claude",
    "timeout": 3600000,
    "maxOutputSize": 10485760,
    "dangerouslySkipPermissions": false,
    "forceAllowRoot": false,
    "runAsUser": null
  },
  "tools": {
    "defaultTool": "opencode",
    "definitions": {
      "opencode": {
        "command": "opencode-cli",
        "args": ["run", "--format", "json", "{prompt}"],
        "versionArgs": ["--version"],
        "description": "OpenCode CLI (Local LLM)"
      },
      "claude": {
        "command": "claude",
        "args": ["--dangerously-skip-permissions", "--print", "{prompt}"],
        "versionArgs": ["--version"],
        "description": "Anthropic Claude CLI",
        "supportsSkipPermissions": true
      },
      "codex": {
        "command": "codex",
        "args": ["exec", "--sandbox", "danger-full-access", "{prompt}"],
        "versionArgs": ["--version"],
        "description": "OpenAI Codex CLI",
        "provider": null,
        "model": null
      },
      "vibe-local": {
        "command": "vibe-local",
        "args": ["-y", "--prompt", "{prompt}"],
        "versionArgs": ["--version"],
        "description": "vibe-local CLI (Ollama)"
      },
      "takt": {
        "command": "takt",
        "args": ["--pipeline", "--task", "{prompt}"],
        "versionArgs": ["--version"],
        "description": "TAKT Agent Orchestrator"
      }
    }
  },
  "server": {
    "port": 3000
  },
  "storage": {
    "dataFile": "channel-repos.json",
    "repositoriesDir": "repositories"
  },
  "logging": {
    "level": "info",
    "format": "human"
  }
}
